rlhelp (Run-Live Help)English | Русский<a name="english"></a>🇬🇧 Englishrlhelp is an interactive command-line utility written in Rust. It automatically parses help text (--help or man) of any console command and converts it into a convenient TUI (Terminal User Interface).No more scrolling through long man-pages or grepping --help output to find a specific flag. Just select the options with checkboxes and run the command.Features🚀 Auto-Parsing: Extracts flags and descriptions from --help. If a command doesn't support --help, it automatically falls back to man pages.🛡️ Freeze Protection: Smart timeout system prevents hanging when trying to get help from interactive programs (like vim or sl).⌨️ TUI Interface: Easy navigation via keyboard (Arrow keys or Vim-bindings).🌐 Language Switch: Force English help (LC_ALL=C) if your system locale provides confusing translations.📋 Live Preview: See the final command string in real-time before execution.InstallationRequires Rust (Cargo).cargo install --path .
UsageSimply provide the command name you need help with:rlhelp <command_name>
# Examples:
rlhelp ls
rlhelp git
rlhelp grep
Key BindingsKeyAction↑ / kMove up↓ / jMove downSpaceToggle flag selectionEnterExecute command with selected flagspPrint command to console and exit (dry run)lToggle help language (System / English)q / EscQuit<a name="russian"></a>🇷🇺 Русскийrlhelp — это интерактивная утилита командной строки, написанная на Rust. Она автоматически парсит справку (--help или man) любой консольной команды и превращает её в удобный TUI-интерфейс.Больше не нужно скроллить длинные man-страницы или грепать --help, чтобы найти нужный флаг. Просто выберите опции галочками и запустите команду.Возможности🚀 Автоматический парсинг: Извлекает флаги и описания из --help. Если команда не поддерживает --help, автоматически ищет man-страницу.🛡️ Защита от зависания: Умная система таймаутов предотвращает зависание при попытке получить справку от интерактивных программ (например, vim или sl).⌨️ TUI Интерфейс: Удобная навигация клавиатурой (стрелки или Vim-биндинги).🌐 Переключение языка: Возможность принудительно запросить английскую справку (LC_ALL=C), если системная локаль выдает некорректный текст.📋 Предпросмотр: Видите итоговую команду в реальном времени перед запуском.УстановкаТребуется установленный Rust (Cargo).cargo install --path .
ИспользованиеПросто укажите имя команды, для которой нужна справка:rlhelp <имя_команды>
# Например:
rlhelp ls
rlhelp git
rlhelp grep
Управление клавишамиКлавишаДействие↑ / kВверх по списку↓ / jВниз по спискуSpaceВыбрать / Снять выбор флагаEnterВыполнить команду с выбранными флагамиpНапечатать команду в консоль и выйти (без выполнения)lПереключить язык справки (System / English)q / EscВыходLicense / ЛицензияDistributed under the MIT License. See LICENSE for more information.